# Reppy — Plan de mejora (auditoría 2026-07-23)

Resultado de una auditoría de 5 ejes: ingeniería, game design, engagement, SEO/growth y mercado.

**Notas**: Producto 7.5 · Engagement 6.5 · Ingeniería 6 · Game design 5.5 · SEO/Growth 4.5 · Negocio 4.5

**Veredicto**: la app NO es cutre — el cuello de botella no es el código, es distribución + balance económico + retención de caídos. Plan: apuesta acotada de 6 meses.

**Kill criterion (revisar ~2027-01-23)**: ~1.000 MAU y D30 > 10%. Si no → hobby sin mala conciencia.

**❌ MONETIZACIÓN DESCARTADA (decisión de Roman, 2026-07-26)**: Reppy NO se monetiza de ninguna forma — ni suscripción, ni compras, ni cosméticos de pago, ni ads. No proponer monetización en futuras auditorías; ignorar cualquier recomendación de los informes originales en esa dirección. La conversión de pago YA NO es parte del kill criterion.

**Regla de oro durante el plan**: 🧊 congelar rediseños visuales y features nuevas. El producto ya sobra para validar.

---

## 🤖 Protocolo de agentes autónomos (routines nocturnas)

**Este fichero en `origin/main` es la memoria compartida entre runs** (cada run es una sesión nueva sin memoria de las anteriores). Los agentes procesan las tasks de arriba a abajo — el orden del fichero es la prioridad.

Estados de una task:

- `- [ ]` — pendiente: cualquier agente puede cogerla.
- `- [ ] 🔨 PR #N` — implementada en una PR abierta pendiente de review de Roman. Los agentes NO la reimplementan; solo verifican o continúan esa PR.
- `- [ ] ⏸️ PREGUNTA: ...` — bloqueada esperando decisión de Roman (la pregunta, 2-3 opciones y una recomendación van escritas inline). Los agentes la saltan sin repetir la pregunta.
- `- [x]` — hecha y mergeada.
- `👤` — acción personal de Roman (compras, cuentas, grabar contenido, enviar emails). Los agentes nunca la ejecutan; como mucho preparan material de apoyo en una PR si es trivial hacerlo.

Protocolo de cada run:

1. **Sincronizar**: leer este fichero en `origin/main` + `gh pr list --state open` + ramas `night/*` remotas.
2. **Verificar antes de crear**: PRs `night/*` a medias → retomarlas y terminarlas. Terminadas sin verificar → verificar (build de frontend si se tocó frontend, `node --check` en backend, revisión lógica del diff) y dejar veredicto como comentario en la PR.
3. **Bucle hasta que muera la sesión** — para cada task pendiente, en orden:
   - **Implementable sin decisiones** → rama `night/<slug-corto>` desde `origin/main`, implementar, verificar, abrir PR contra `main`, y actualizar ESTE fichero en `main` con `🔨 PR #N` (commit directo a main SOLO de este fichero).
   - **Requiere decisión de producto** → NO implementar a ciegas: escribir el `⏸️ PREGUNTA: ...` en este fichero (commit a main) y pasar a la siguiente.
   - **`👤` o `⏸️`** → saltar.
   - Push temprano y frecuente, WIP incluido: si la sesión muere a medias, lo pusheado lo retoma el siguiente run.
4. **A `main` solo se commitea este fichero (estado). Código, SIEMPRE por PR. Nunca mergear PRs** — las revisa y mergea Roman.
5. **Convenciones**: manda `CLAUDE.md` (i18n en ambos locales es/en, rarezas en español, `withTransaction` + `FOR UPDATE` en economía).
6. **Verificación — intentar SIEMPRE el test de integración local en el sandbox**: instalar/arrancar PostgreSQL efímero (`apt-get install postgresql` o Docker, lo que el entorno permita), cargar `backend/schema.sql`, arrancar el backend con env dummy — solo hacen falta `DATABASE_URL` (al Postgres local) y `JWT_SECRET` (cualquier string): Pusher, VAPID/web-push, Google OAuth y Hevy tienen guards y degradan con un warning sin crashear, así que NO inventar claves para ellos y probar con `curl` los endpoints tocados (crear usuario de prueba vía `/api/auth`, generar JWT, ejercitar el flujo). Si el entorno no permite levantar Postgres → degradar a `npm run build` (frontend), `node --check` (backend) y revisión lógica. En la PR, decir SIEMPRE qué nivel de verificación se consiguió: `integración local` / `solo build+sintaxis`. PROHIBIDO testear contra https://reppy.romandev.app o la BD de producción: ese entorno corre el código viejo de main y tiene datos y usuarios reales.
7. 🧊 Nada fuera de la lista: ni rediseños, ni refactors oportunistas, ni features nuevas.
8. **Si NO quedan tasks accionables** (todas están `[x]`, `🔨`, `⏸️` o `👤`) → el run se convierte en **auditor**: elige el eje menos recientemente auditado de estos cinco — ingeniería/seguridad, game design/economía, engagement/retención, SEO/rendimiento web, deuda técnica — (el registro de auditorías está al final de esta sección), re-audita el código actual A FONDO en ese eje (mismo nivel de exigencia que la auditoría original de este fichero: leer código real, citar archivo:línea, severidad, sin peloteo), y añade las tasks nuevas que encuentre en una sección `## 🔍 Backlog propuesto (auditoría <eje> <fecha>)` al final del fichero, cada una marcada `⏸️ PENDIENTE DE APROBACIÓN` con justificación de una línea. Después actualiza el registro de abajo y termina el run — las tasks propuestas NO se implementan hasta que Roman las apruebe (les quite el ⏸️ y las mueva a su sección de prioridad).

**Registro de auditorías por eje** (actualizar al auditar): ingeniería/seguridad 2026-07-27 · game design/economía 2026-07-26 · engagement/retención 2026-07-26 · SEO/rendimiento web 2026-07-27 · deuda técnica 2026-07-27.

**Flujo de mañana (Roman)**: revisar/mergear PRs `night/*`, marcar `[x]` las mergeadas, responder las `⏸️` (editando aquí o diciéndoselo a Claude en sesión local, que quitará el emoji) → la noche siguiente se implementan.

---

## P0 — Seguridad (esta semana, explotable HOY en producción)

- [x] **Matar `/api/test/*` en producción** (PR #305 mergeada) — `backend/test.js:9` (`isTestAllowed = true` incondicional) + montaje en `backend/index.js:157`. Cualquier usuario autenticado puede darse coins/gems/cofres/items/stats infinitos. *Esfuerzo: 10 líneas.*
- [x] **Autenticar `/pusher/auth`** (PR #306 mergeada) — `backend/index.js:170-197` acepta `user_id`/`user_name` arbitrarios y autoriza canales private-/presence-. Suplantación de usuarios en realtime. Eliminar también el shim legacy `api/pusher-auth.js` (CORS `*`). *Esfuerzo: 10 líneas.*
- [x] **Rate limiting** (PR #307 mergeada) — no existe en ningún endpoint. Mínimo: `express-rate-limit` en `POST /auth/login` (brute-force) y `POST /reps` (spam). *Esfuerzo: 1-2 h.*
- [x] **Tope a `count` y validar `date` en `POST /reps`** (PR #308 mergeada) — `backend/reps.js:49-103`: hoy acepta `count: 99999999` (monedas/XP/daño infinito) y fechas arbitrarias (rachas retroactivas). Sanity cap + `date` solo hoy/ayer. *Esfuerzo: 1 h.*
- [x] **Quitar CORS `*.vercel.app`** (PR #309 mergeada) — `backend/index.js:92`: cualquiera puede desplegar gratis en vercel.app y pasar el allowlist con `credentials: true`. Ya no estáis en Vercel. *Esfuerzo: 5 min.*

## P0 — Promesas rotas al jugador (bugs de diseño, ~2-4 h en total)

- [x] **Pagar los `rewards` de nodos de campaña** (PR #312 mergeada) — `backend/data/campaigns/main-campaign.json` promete 1.000/2.500 coins por trono/finale; `applyCampaignDamage` (campaignEngine) nunca los otorga.
- [x] **XP de quests de NPC ignorado** (PR #313 mergeada) — `backend/utils/campaignQuests.js` `claimQuest` paga coins/gems/buff pero el campo `xp` del JSON es letra muerta. Pagarlo o quitarlo del JSON. *(Se optó por pagarlo en `cha_xp`, la bolsa de XP persistente que ya usa training.js.)*
- [x] **Misión `buy_legendary` incompletable** (PR #310 mergeada) — `backend/shop.js:530` compara `'Legendary'`/`'Calisthenics'` contra rarezas reales `legendary`/`calistenico`. Nunca matchea.
- [x] **`scripts/seed_rpg_items.js` con rarezas en inglés** (PR #311 mergeada) (`special`, `calisthenic`) que las queries de cofres (`WHERE rarity = 'especial'`) no encuentran → items huérfanos.
- [x] **Cofres de nivel: renombrar a genérico** ✅DECIDIDO (2026-07-25: opción A) — renombrar "Cofre de Nivel" → nombre genérico ("Cofre de Batalla") en DB + i18n (es/en) + premio de ruleta (`roulette.js`) + UI. Se mantiene como recompensa de ruleta/tienda; NO se restaura el cofre por subir de nivel (`additionalChests` sigue en 0). Coherencia i18n en ambos locales.

## P1 — Rebalance económico (mes 1, empieza por una hoja de cálculo)

> Hoy entrenar aporta solo el ~10-15% del ingreso diario de un jugador con racha. La app premia abrir, no entrenar.

> **Base de datos para decidir**: PR #314 añade `docs/economy-curves-2026-07.md` + `backend/scripts/analyze_economy_curves.js` (curvas reales). Los ⏸️ de abajo referencian esos números. Todos son decisiones de balance — no se implementan hasta que Roman elija.

- [x] **Cap a la recompensa de racha** ✅DECIDIDO (2026-07-25: opción A) — `backend/utils/stats.js:300`: cambiar `streak × 50` por **`50 + 5×min(streak,30)`** (cap 200 monedas/día). Verificar que no rompe ningún otro cálculo que dependa de la recompensa de racha.
- [x] **EV de la ruleta 4h: SIN CAMBIOS** ✅DECIDIDO (2026-07-25) — Roman decide dejar la ruleta 4h como está. No tocar `roulette.js` ni `LuckyWheel.vue` por este motivo.
- [x] (Parte A) **Domar la bola de nieve de FE** ✅DECIDIDO (2026-07-25: opción A+C) — `backend/utils/damage.js`: bajar el bono plano `divineBonus = fthLvl × 25` → **`fthLvl × 5`**. Y romper el bucle daño→FE de `stats.js:193`: que la XP de FE NO venga del daño infligido, sino de participar en raids/quests (definir la nueva fuente al implementar). Cambio de feel del combate — PR revisable, no auto-merge.
  - **Parte A (bono plano ×25→×5) → PR #326** (también aplica a `baseDivine` para no descuadrar el desglose base/gear). Sin impacto retroactivo (solo cambia el daño futuro).
  - **Parte C ✅ RESUELTA (2026-07-27, Roman): opción (C) — NO se implementa, task cerrada.** La Parte A (#326, bono divino ×25→×5) ya cortó la realimentación dominante del bucle daño→FE; no se toca la fuente de `fth_xp` y se evita el desnivel retroactivo. *Análisis original que llevó a la decisión:* cambiar la fuente de `fth_xp` NO es aislable. `stats.js:193` (`fth_xp = daño/50`) alimenta `rawTotalXP` (`stats.js:235`) → `total_xp` → **el NIVEL GLOBAL** (`stats.js:240-241`), y `recalculateUserStats` recalcula desde el historial y **permite que el nivel baje**. Poner FTH = "participación" (nº mucho menor que daño/50 para veteranos) **desnivelaría retroactivamente a TODOS los usuarios** en su siguiente log (y el nivel global realimenta daño, leaderboards, etc.). Opciones: **(A)** implementar la nueva fuente + añadir un término de compensación para que `total_xp` no caiga (p. ej. congelar el `fth_xp` histórico actual como un `fth_legacy_xp` de solo-lectura y sumar la participación encima) — conserva niveles pero es más código; **(B)** aceptar el reseteo/desnivel como parte del rebalance y comunicarlo; **(C)** dejar FTH ligado al daño pero SOLO el bono plano nerfeado (Parte A) ya rompe la mayor parte del bucle → quizá la Parte C no haga falta. **Recomendación del agente: (C)** — la Parte A ya corta la realimentación dominante (el término plano) sin tocar niveles; medir antes de arriesgar un desnivel masivo. Roman decide.
- [ ] 🔨 PR #346 **Nerfear pociones DEX de crit: subir precio a 40.000** ✅DECIDIDO+NÚMERO (2026-07-26, Claude) — subir el precio de la poción calisténica `+100 dex` de ~12.000 a **40.000 monedas** (×3.3) en el seed `backend/archive/diversify_consumables.js` (y en `items` si ya está sembrada, vía migración/update). Deja de ser outlier de ratio daño/coste. Accionable ya.
  - **✅ RESUELTA (2026-07-27, Roman): precio 40.000 · `UPDATE` a la tabla `items` de prod AUTORIZADO · sube TAMBIÉN "Sangre de Dragón" (legendary, +50 dex) de 6.000 → 13.000** para no crear un outlier un escalón abajo. *Implementación*: script one-shot (patrón `backend/tmp/update_prices_pg.js`) con los dos `UPDATE items SET price=$1 WHERE name=$2`, MÁS actualizar el seed `archive/diversify_consumables.js` para futuras siembras. *Análisis original:* dos cosas sin cerrar. **(1) Precio exacto**: la poción es *"Maestría del Cuerpo"* (`calistenico`, +100 dex, 24h), **precio actual 18.000 coins**. ×3 = 54.000 · ×4 = 72.000. **(2) Mecanismo**: el seed `archive/diversify_consumables.js` está archivado y **NO se ejecuta en runtime** — editarlo NO cambia el precio en producción (los items viven en la tabla `items`, ya sembrada). Para que tenga efecto real hace falta un one-shot `UPDATE items SET price=$1 WHERE name='Maestría del Cuerpo'` (patrón como `backend/tmp/update_prices_pg.js`), aparte de actualizar el seed para futuras siembras. **Recomendación del agente: ×4 (72.000) + script de migración one-shot + actualizar el seed**; y decidir si subir también proporcionalmente *"Sangre de Dragón"* (legendary, +50 dex, 6.000) para no crear un nuevo outlier un escalón abajo, o dejarla. **Roman confirma precio(s) y si autoriza el `UPDATE` a la tabla `items` de prod → la noche siguiente se implementa.**
- [ ] 🔨 PR #347 **Arreglar la curva de dificultad** ✅DECIDIDO+NÚMERO (2026-07-26, Claude) — HP enemigo pasa de lineal `1+0.05·L` a **cuadrático**: `hp_scale = 1 + 0.05·L + 0.005·L²` (casa con el daño superlineal del jugador). **Objetivo de calibración**: que matar un enemigo estándar cueste ~25-40 reps a CUALQUIER nivel (usar `docs/economy-curves-2026-07.md` para afinar el 0.005). ADEMÁS poner **tope al multiplicador de crit** (el auditor señaló crit sin tope): cap del daño de crit a ×3 sobre el golpe base. Toca `damage.js` (crit) + la fórmula de HP (`campaignEngine.js`/`boss.js`). PR revisable.
  - **✅ RESUELTA (2026-07-27, Roman): SOLO HP, no se toca el crítico.** `scaleEnemyHp` pasa a `base·(1 + 0.05·L + 0.005·L²)·tierMult·prestigeMult`, afinando el coeficiente cuadrático contra `docs/economy-curves-2026-07.md` para que matar un enemigo estándar cueste ~25-40 reps. `critMult = 2 + dex·0.1` se queda SIN tope: se asume que la estabilidad es real en el tramo medio (1-50) y que a nivel alto los críticos siguen desbocados. *Análisis original:* la fórmula vive en `scaleEnemyHp` (`backend/utils/campaignEngine.js:28`): `base·(1 + lvl·0.05)·tierMult·prestigeMult`. Dos bloqueos antes de derivar coeficientes: **(1) La curva de daño está EN OBRAS ahora mismo** — PR #325 (racha), PR #326 (divino ×25→×5) y la Parte C de FE (fuente de `fth_xp`, ⏸️) cambian el daño; fijar el HP contra una curva que va a moverse sería fitting a un blanco móvil. Hay que derivar el HP **después** de que aterrice el rebalance de daño. **(2) Un HP cuadrático NO basta para estabilizar las reps-para-matar en todo el rango**: el `analyze_economy_curves.js` muestra que el daño esperado-con-crítico es ~cúbico porque `critMult = 2 + dex·0.1` crece **sin tope** (nivel 100: dmg noCrit 50.695 vs expCrit 496.811, ~10× solo por crítico). Un `L²` de HP seguiría al término multiplicativo de nivel/FE pero no a la explosión de crítico. → Para "estables de verdad" probablemente haga falta **también** capar `critMult` (fuera del scope actual) o aceptar estabilidad solo en el tramo medio. **Recomendación del agente**: (a) mergear primero #325/#326 y decidir la Parte C de FE; (b) luego derivar `scaleEnemyHp` como `base·(1 + a·L + b·L²)·tierMult·prestigeMult` fijando reps-to-kill≈objetivo en el tramo 1–50 con la curva de daño final; (c) decidir aparte si capar el crítico. **Roman**: ¿objetivo de reps-para-matar (constante ~N, o subiendo suave)? ¿autorizas tocar el crítico o solo el HP? El HP se snapshotea al engage, así que el cambio NO afecta batallas ya empezadas.
- [ ] 🔨 PR #348 **Prestigio con ROI positivo: cosméticos exclusivos** ✅DECIDIDO+CONTENIDO (2026-07-26, Claude) — crear **6 cosméticos `prestige_only`** (nueva flag/columna en `items` o cosmetics), otorgados **1 por vuelta de prestigio** (NG+1 → el 1º, NG+2 → el 2º…): (1) Título «El Reencarnado», (2) Marco «Aura de Prestigio», (3) Efecto de partículas «Brasas Eternas», (4) Título «Sin Fin», (5) Marco «Halo Carmesí», (6) Efecto «Estela Dorada». No comprables con monedas/gemas (exclusividad = el gancho). Sembrar en un seed + gate en el evento de prestigio. PR revisable.
  - **✅ RESUELTA (2026-07-27, Roman): set FIJO de 5 títulos «Renacido I…V» + flag `is_exclusive`.** Uno por vuelta de prestigio (NG+1 → *Renacido I* … NG+5 → *Renacido V*); a partir de NG+6 no hay cosmético nuevo. Gradientes escalando **bronce → plata → oro → carmesí → prismático**. Se añade `cosmetics.is_exclusive BOOLEAN DEFAULT false` y se EXCLUYE de la query de la tienda. Grant con `INSERT INTO user_inventory` dentro de `prestige()` (`campaignEngine.js:328`). Necesita seed/migración que corra en prod, no solo JSON. *Análisis original:* el enganche técnico es fácil (`prestige()` en `campaignEngine.js:328` ya calcula `newLevel`; bastaría un `INSERT INTO user_inventory(user_id, cosmetic_id)` con el cosmético de esa vuelta). Lo que falta son **3 decisiones de Roman**: **(1) Contenido** (creativo, es la recompensa entera): nombres + `css_value` (gradientes/efectos) + `type` de cada cosmético. Propuesta de arranque: títulos `Renacido I…V` con gradientes escalando (dorado→plateado→prismático) — Roman aprueba/reemplaza. **(2) Exclusividad**: la tabla `cosmetics` no tiene marca de "no comprable"; hoy todo cosmético tiene `price` y sale en la tienda. Hay que añadir un flag (`is_exclusive BOOLEAN` / `source='prestige'`) y **excluirlo de la query de la tienda**, o convención `price = -1`. → decisión de diseño de schema. **(3) Mecanismo de siembra**: los cosméticos viven en la tabla `cosmetics` (mismo caso que las pociones) → hace falta migración/seed que corra en prod, no solo un JSON. **(4) ¿Un cosmético por nivel de prestigio (infinito) o un set fijo (p. ej. 5) y luego repetir/monedas?** **Recomendación**: set fijo de 5 títulos exclusivos + flag `is_exclusive` + grant en `prestige()`. Roman confirma nombres/estilos y el flag → se implementa.
- [x] **Hoja de cálculo de curvas** (PR #314 mergeada) (daño/HP/monedas por nivel y día) antes de tocar números — hecho: `docs/economy-curves-2026-07.md` + `backend/scripts/analyze_economy_curves.js`.

## P1 — Retención (mes 1, el mayor ROI de toda la auditoría)

> Winback actual = cero: un usuario que falla UN día no recibe nada nunca más. D30 estimado hoy: 3-8%. Con estos dos primeros puntos: 10-15%.

- [x] **Winback cron D3/D7/D14** (PR #317 mergeada) — reutilizar la infra de `backend/utils/streakReminders.js` con query de usuarios sin reps en N días y su dato real ("tenías racha de 12"). *~1 día.*
- [x] **"Día de descanso activo"** ✅DECIDIDO (2026-07-25: opción A) — botón diario que **preserva la racha 1×/semana gratis** (sin coste en monedas, aparte del freeze de 250 que se mantiene). Requiere nuevo mecanismo + tabla/columna de uso semanal del descanso (reset semanal). No toca la economía del freeze. i18n es/en. Cambia la mecánica core de racha → PR revisable. *(1 descanso gratis/semana; ajustar si hace falta.)*
- [x] **Matar/segmentar el blast de referral** (PR #315 mergeada) — `backend/utils/referralReminders.js` (días 1 y 16 a TODOS): spam que quema el canal push. Solo activos >7 días que nunca refirieron, máx 1 vez.
- [x] **Push "tu rival te adelantó"** (PR #316 mergeada) — la query ya existe en `backend/social_feed.js` `/stats`; dispararla 1×/día. Reutilización pura.
- [x] **Reto vs tu semana pasada (versión completa)** ✅DECIDIDO (2026-07-25) — meta = tus **reps de la semana previa**; al igualar/superarla, recompensa fija pequeña (**1 cofre común/de batalla**); **card en el Dashboard** mostrando progreso vs semana pasada; reset semanal (lunes). Reutiliza la query semanal ya usada en `/social-feed/stats`. Es la única "feature nueva" aprobada del backlog. i18n es/en. PR revisable.
- [x] **Eventos GA4 de funnel** (PR #318 mergeada) — hoy solo pageviews: añadir `signup`, `first_log`, `push_enabled`, `spin`, `day2_return`. *Una tarde.* Quitar `@vercel/analytics` de `frontend/src/main.js` (muerto tras Coolify).

## P1 — Legal / tema

- [ ] ⏸️ / 👤 **Renombrar los bosses con IP ajena** — APLAZADO (2026-07-25: Roman decide dejarlo para después). Riesgo legal asumido mientras tanto. Nombres Y arte infringen IP: `backend/index.js` `/db/init` inserta Artorias/Nameless King (Dark Souls), The Ender Dragon (Minecraft), Rathalos (Monster Hunter), Baldur (God of War), Arthas (Warcraft), Malenia (Elden Ring), Sephiroth (FF), Calamity Ganon (Zelda), Diablo — con arte de `static.wikia.nocookie.net`. Bloqueado por: (1) nombres/lore propios (creativo, Roman), (2) arte original (👤). *Nombres propuestos si se retoma*: Abisario el Caído, El Devorador de Descansos, El Rey Sin Nombre, Vórtice Final, La Invicta de la Barra, Coloso Osteo, Ala Rota, Fauces del Averno, Cronarca. **Los agentes nocturnos NO cogen esta task.**

## P2 — Combate con decisiones (el cambio con mejor ratio esfuerzo/impacto del juego)

- [x] PR #336 **Combate: ejercicio → daño vs weakness/resist** ✅DECIDIDO+TABLA (2026-07-26, Claude; opción B) — MANTENER el bono por nivel de stat Y AÑADIR un multiplicador por el ejercicio logueado. **Mapeo grupo-muscular (Hevy `primary_muscle_group`, ver `backend/data/hevy_exercise_templates.json`) → stat del juego**: **STR** = chest, lats, upper_back, lower_back, triceps, biceps, forearms, traps, shoulders · **VIG** = quadriceps, hamstrings, glutes, calves, abductors, adductors · **END** = abdominals, cardio, full_body, neck, other. (DEX/INT/FTH/CHA no mapean a músculo — los cubre el bono por nivel existente.) **Multiplicadores** (ADEMÁS del bono actual): stat-del-ejercicio == `weakness_stat` → **×1.4**; == `resist_stat` → **×0.7** (esto ACTIVA `resist_stat`, hoy muerto); si no, ×1.0. Toca `damage.js:130-137` + necesita saber el grupo/stat del ejercicio logueado (los ejercicios de Reppy ya tienen `stat_type`; los importados de Hevy mapean vía grupo). PR revisable. **MERGEADA 2026-07-27** (commit `4c1847b`). ⚠️ **CORRECCIÓN DE PREMISA (2026-07-27, Roman+Claude)**: la frase «`resist_stat`, hoy muerto» era FALSA — ya había un penalizador por NIVEL de stat resistido en `campaignEngine.js` (`damage / (1 + min(0.5, lvl×0.01))`). Los dos se apilaban: ×0.7 × ×0.667 = **×0.47** a nivel alto, y peor aún, el de nivel se aplicaba **aunque el ejercicio logueado NO tocase el resist** (castigo por tener el stat subido, incentivo invertido). **Resuelto quitando el penalizador por nivel**; el resist ahora depende sólo de lo que ENTRENAS, no de lo que ya tienes construido.
- [ ] 🔨 PR #349 ✅DECIDIDO+NÚMERO (2026-07-27, Roman) **Boss comunitario: HP escalado por usuarios activos, SIN ventana ni cron** — `backend/utils/boss.js`: HP hardcodeado 50.000 (escalado por usuarios calculado y descartado), un veterano lo funde en una sesión y se auto-resetea dentro de un GET. Darle horario, anticipación y anuncio.
  - **✅ RESUELTA (2026-07-27, Roman): `newTotalHp = max(50000, activeUsers × 400)`, el boss sigue SIEMPRE activo.** Se descartan las ventanas programadas, el `node-cron`, el countdown y el push de anticipación: NO se toca `start_date`/`end_date` ni se añade UI. El único cambio es dejar de descartar el `activeUsers` que `syncBossHealth` ya calcula. Los multiplicadores `is_legendary` (×8) / `is_epic` (×3) se mantienen encima. Con la base actual el `max()` lo deja en 50.000 hasta pasar de ~125 activos semanales. *Análisis original:* `syncBossHealth` calcula `activeUsers` (COUNT distinct reps últimos 7 días) pero lo **descarta**: `newTotalHp = 50000` fijo, escalado solo por flags `is_legendary`(×8)/`is_epic`(×3); se llama al matar un boss (`combat.js`) → auto-reset inmediato, sin ventana ni anuncio. `boss_fights` ya tiene columnas `start_date`/`end_date` sin uso real. Faltan **3 decisiones de Roman**: **(1) Cadencia** — ¿boss siempre activo (hoy) o ventanas programadas (p.ej. 1 boss/semana vie–dom via `node-cron`)? **(2) Fórmula de HP** — ¿reactivar el escalado por `activeUsers` para que un veterano no lo funda solo (p.ej. `max(50000, activeUsers × N)`), con qué N? **(3) Anuncio** — ¿push existente + countdown en Dashboard reusando `start_date`/`end_date`? **Recomendación**: ventana semanal + `HP = max(50000, activeUsers × 400)` + countdown en Dashboard + push de anticipación. Roman fija cadencia y N.
- [ ] 🧊 APARCADA (2026-07-27, Roman) **Integridad competitiva mínima** — soft-cap diario de reps con rendimientos decrecientes + badge "verificado" para datos de Hevy + leaderboards separados verificado/manual.
  - **🧊 DECISIÓN (2026-07-27, Roman): NADA por ahora — task congelada.** Con la base de usuarios actual el problema es teórico; no se implementa ni el soft-cap ni la columna `reps.source` ni los leaderboards separados hasta que haya volumen suficiente para que alguien tenga incentivo real a inflar números. **Los agentes nocturnos NO cogen esta task.** *Análisis original (válido cuando se retome):* bloqueo técnico clave — la tabla `reps` **NO distingue origen**: Hevy y manual hacen upsert a la MISMA fila (`UNIQUE user_id,date,exercise_type`); solo `hevy_imported_workouts` registra las importaciones aparte. Los leaderboards (`leaderboard.js`/`profile.js`/`social_feed.js`) ordenan por reps sin filtro de origen, y no hay soft-cap. Faltan decisiones: **(1) Soft-cap** (no necesita schema, es puro cálculo de recompensa en `reps.js`/`rewards.js`): umbral + curva, p.ej. 100% hasta 200 reps/día · 50% 200–400 · 10% >400 — ¿números? **(2) Badge "verificado"**: requiere **columna nueva de origen en `reps`** (`source`/`verified`) capturada en `reps.js` (manual) y `hevyIngest.js` (hevy); derivarlo de `hevy_imported_workouts` es frágil por el merge de filas. ¿Autorizas el cambio de schema? **(3) Leaderboards separados**: depende de (2). **Recomendación**: implementar SOLO el soft-cap primero (sin schema); posponer badge + leaderboards separados a un segundo PR que añada `reps.source`. Roman fija números del soft-cap y si autoriza la columna.

## P2 — Distribución (mes 2-6, el experimento que decide todo)

- [ ] 👤 **Comprar dominio propio** (`reppy.app` / `reppy.fit`, ~10-40 €/año) + 301 desde `reppy.romandev.app`. Con ~0 tráfico la migración es una tarde; nunca será más barata que ahora.
- [ ] **Play Store vía TWA/Capacitor** (Bubblewrap, $25 una vez) — quien busca "app dominadas" espera acabar en una store. Android primero (calistenia hispana).
- [ ] 👤 **TikTok/Shorts 2-3/semana** — el canal principal. Formatos: "mis dominadas dañan al boss", boss comunitario en vivo ("muere cuando la comunidad haga 10.000 flexiones"), progresos de usuarios.
- [ ] 👤 **Product Hunt + Show HN** — gratis, pico de un día y backlinks DR alto (lo que le falta al dominio).
- [ ] 👤 **Outreach mensual a listículos** — FitCraft, Bitletics, RazFit, MainQuest, AlternativeTo, listas "Habitica alternatives". Un email por sitio = backlink + tráfico de intención perfecta.

## P2 — SEO fixes (mantenimiento, no canal principal)

- [x] (parcial) **Soft-404 global** — cualquier URL inventada devuelve 200 con la home (canonical a `/es`). Fallback 404 real o `noindex` en el catch-all del router + config Traefik/Coolify. **PR #338 hace la parte code-side (`noindex` en la ruta `not-found` del router)**; queda 👤/infra el **404 real por status HTTP** (config Traefik/Coolify), que no es implementable desde el código.
- [x] **`/` → 301 a `/es`** (hoy 200 con canonical). **Sí era implementable por código**: `frontend/nginx.conf` está versionado y es el que sirve el frontend (`frontend/Dockerfile:26`), no hacía falta tocar Traefik. La PR hace el 301 con un `map` sobre `Accept-Language` (navegador en inglés → `/en`, todo lo demás **incluidos los crawlers** → `/es`) para no romper la detección de idioma que hoy hace el router (`router.js:33` + `i18n.js:16`, que manda a `/en` a cualquier navegador no-español). Verificado con nginx real en local.
- [x] **`llms.txt` con slugs ES bajo `/en/blog/`** — `frontend/public/llms.txt:29-38`. *(Fix: los 10 enlaces EN ahora usan el `slugEn` real de `blogPosts.json`, coincidiendo con las rutas SSG de `vite.config.js:110`.)*
- [x] **Limpiar `frontend/index.html`** (PR #320 mergeada) — meta keywords con ~45 términos (línea 14) y bloque `<noscript>` keyword-stuffed (178-230), redundante con SSG.
- [x] **Sitemap de atletas: umbral ≥50 reps** ✅DECIDIDO (2026-07-25: opción A) — subir el filtro de `top-public` a `total_reps >= 50` en `backend/profile.js:106` (un WHERE). Solo perfiles con actividad real se exponen a Google. Bajo riesgo, accionable ya.
- [x] + #341 + #343 + #344 + #345 (parcial: 5/10) **Engordar los 10 mejores posts a 1.500-2.500 palabras** (empezar por clúster dominadas ES) y **dejar de producir posts de 300 palabras** — el 80% del blog actual es thin content que no rankea. **#340** engorda el pilar `guia-definitiva-dominadas` (272 → ~1.7k, intención "primera dominada"); **#341** engorda el pilar `error-mas-dominadas` (247 → ~1.5k, intención "hacer más dominadas"); **#343** engorda el pilar `programa-dominadas-8-semanas` (801 es / **416 en** → ~2.4k / ~2.3k; el EN era la mitad que el ES, ahora hay paridad). **#344** engorda `dolor-antebrazo-codo-dominadas` (958/514 → ~1.86k / ~1.75k). **#345** engorda `grease-the-groove-mas-dominadas` (672/641 → ~1.72k / ~1.67k). **Con esto el clúster dominadas queda cubierto entero** salvo `dolor-codo-dominadas`, bloqueado por la ⏸️ de canibalización de abajo. Quedan ~5 posts (otros pilares fuera del clúster: mirar `pike-push-ups`, `manual-inicio-calistenia`, `rutina-calistenia-semanal-principiantes` y los thin de flexiones/fondos) para futuras noches. **Patrón detectado**: varios posts tienen el EN mucho más corto que el ES (el pilar del programa tenía 801/416 y este 958/514) — revisar la paridad al engordarlos, no solo el recuento en español.
  - **🔨 PR #350** · **✅ RESUELTA (2026-07-27, Roman): opción (A) — consolidar con 301.** `301` de `/es/blog/dolor-codo-dominadas` → `/es/blog/dolor-antebrazo-codo-dominadas` (y el par EN) en `frontend/nginx.conf`, con el mismo patrón que ya existe ahí para `rutina-pike-pushups` (`nginx.conf:30-38`), **y sacar el post corto del dataset** (`backend/blogData.json` → se propaga a `frontend/src/blogPosts.json` vía `sync-blog`). No se pierde contenido: lo útil del corto ya está incorporado en #344. *Análisis original:* `dolor-codo-dominadas` (282/264 palabras, `image` de Unsplash, tono RPG) y `dolor-antebrazo-codo-dominadas` (el que engorda #344) **apuntan a la misma query**: la keyword `"dolor codo dominadas"` está literalmente en el array `keywords` de **los dos**. Engordar el corto a 1.500 palabras no arregla nada, empeora la canibalización. Opciones: **(A)** consolidar — 301 de `/es/blog/dolor-codo-dominadas` → `/es/blog/dolor-antebrazo-codo-dominadas` (y el par EN) en `frontend/nginx.conf`, exactamente el patrón que ya existe ahí para `rutina-pike-pushups` → pilar de pike push ups (`nginx.conf:30-38`), y sacar el post del dataset; **(B)** diferenciar intenciones (p. ej. el corto pasa a ser solo prevención/calentamiento y el largo el diagnóstico-tratamiento), lo que exige reescribir los dos y sigue siendo frágil; **(C)** dejarlo como está y asumir que se reparten señales. **Recomendación del agente: (A)** — el contenido útil del corto (anillas, agarre, pulgar fuera) **ya está incorporado en #344**, así que el 301 no pierde nada. Es una decisión de contenido (elimina una URL indexada) → no la ejecuto sin tu OK.

## P3 — Deuda técnica (a medida que se toque, no big-bang)

- [x] **Tests + CI mínimo sobre flujos de economía** — hoy `"test": "exit 1"` y cero CI para una app con dinero virtual. *(Fix: suite `node:test` — math de recompensas + invariantes de concurrencia de las races de #321 contra Postgres — y workflow CI GitHub Actions con servicio `postgres:16`, `node --check` sweep y build SSG. Los tests legacy `tests/*.test.js` quedan fuera del scope, necesitan DB migrada.)*
- [x] **Race conditions restantes**: `backend/shop.js:255-282` (`/daily/refresh` sin transacción ni guard de gemas) y `backend/reps.js:213-230` (referral pagable 2 veces). El patrón correcto está en `roulette.js`. *(Fix: `withTransaction`+`FOR UPDATE` en el refresh, claim gate atómica en el referral; verificado con Postgres efímero local, 6/6 tests de concurrencia.)*
- [ ] ✅DECIDIDO (2026-07-27, Roman: opción A) **Unificar las 3 fuentes de esquema** — `/db/init` (index.js:314-640, usa rareza `epic` inexistente), `schema.sql` y `ensureSchemaMigrations()`. Drift garantizado.
  - **✅ RESUELTA (2026-07-27, Roman): opción (A) — `schema.sql` es la ÚNICA fuente de verdad.** Un único path idempotente en arranque que lo aplica; se retira el DDL duplicado de `GET /db/init` (queda como seed si acaso) y se arregla de paso la rareza `epic` inexistente. Encaja con la Fase 3 de la migración de BD (levantar la BD desde `schema.sql`). **PR revisable, con test de que un arranque contra BD limpia funciona** — afecta al boot del backend, no auto-merge. *Análisis original:* es un refactor con riesgo de corrección (no aislable) → decisión de enfoque antes de tocar. El discovery de migración (`docs/db-migration-discovery.md`, PR #339) confirma las 3 fuentes: `schema.sql` (canónico completo), `GET /db/init` (`index.js:381`, DDL+seeds gated por `DB_INIT_SECRET`) y `ensureSchemaMigrations()` (`index.js:746`, en arranque). Opciones: **(A)** hacer `schema.sql` la ÚNICA fuente + un único path idempotente en arranque que lo aplique, y retirar el DDL duplicado de `/db/init` (dejarlo solo como seed si acaso); **(B)** introducir una carpeta de migraciones versionadas (herramienta tipo node-pg-migrate) y generar el esquema desde ahí; **(C)** dejarlo y solo arreglar el bug puntual (la rareza `epic` inexistente en `/db/init`). **Recomendación: (A)** — barato, elimina el drift, encaja con la Fase 3 de la migración (levantar la BD desde `schema.sql`); arreglar el `epic` de paso. Roman elige enfoque (afecta el arranque del backend, PR revisable con test de que un arranque en BD limpia funciona).
- [ ] **Trocear archivos monstruo al tocarlos** — `Dashboard.vue` (1.829), `Inventory.vue` (1.587), `Shop.vue` (1.317), `backend/training.js` (1.147), handler `POST /reps` (~10 responsabilidades).
- [ ] ✅DECIDIDO (2026-07-27, Roman: opción B) **JWT**: 30 días con `is_admin` embebido y sin revocación — refresh tokens o TTL corto.
  - **✅ RESUELTA (2026-07-27, Roman): opción (B) — `users.token_version` + leer `is_admin`/`is_banned` de la BD.** Columna `token_version INT DEFAULT 0` en `users`, embebida en el payload y comparada en `middleware.js`: subirla invalida TODOS los tokens de ese usuario (logout global, ban, cambio de rol) sin flujo de refresh ni tocar el frontend. Además, las rutas admin dejan de fiarse del claim `is_admin` del token y lo leen de la BD. Se mantiene el TTL de 30d. **PR revisable (cambio de seguridad), no auto-merge.** *Análisis original:* confirmado el riesgo — `generateToken` firma `{ id, is_admin }` con `expiresIn: '30d'` (`auth.js:14`) y el middleware lee `is_admin` **del token**, no de la BD (`middleware.js:22`). Consecuencia: quitar admin a alguien o banearlo **no surte efecto hasta 30 días** (el token viejo sigue siendo admin/válido). Opciones: **(A)** access token corto (15 min) + refresh token rotatorio guardado/revocable en BD — lo correcto pero más código y toca el frontend (renovación); **(B)** mantener un solo token pero añadir `users.token_version` (INT) al payload y comparar en el middleware: subir la versión invalida TODOS los tokens de ese usuario (logout global, ban, cambio de rol) — revocación barata sin flujo de refresh; **(C)** solo bajar el TTL (p.ej. 7d) como mitigación mínima, sin revocación real. Extra en cualquier caso: leer `is_admin` de la BD (no del token) en rutas admin, para no depender del claim viejo. **Recomendación: (B) + leer `is_admin`/`is_banned` de la BD en el middleware admin** — cierra el agujero de escalada/ban sin reescribir el login. Roman elige (cambio de seguridad, PR revisable).
- [x] (comentario; `HEVY_ENCRYPTION_KEY` sigue 👤 infra) **SSL de db.js: documentar y cerrar** ✅DECIDIDO (2026-07-25: opción B) — dejar `rejectUnauthorized:false` (la conexión a Supabase ya va por TLS; solo no se valida el CA — riesgo bajo). Añadir comentario en `backend/db.js:13` explicando el porqué. ADEMÁS (caveat del agente): fijar `HEVY_ENCRYPTION_KEY` en Coolify (hoy cae a `JWT_SECRET`/default de dev). La Hevy key ya se cifra AES-256-GCM ✓. Accionable ya.
- [x] **Borrar shim `api/` + vercel.json** ✅DECIDIDO (2026-07-25: opción B) — borrar el directorio `api/` (shim Vercel muerto, ya estáis en Coolify) y `vercel.json`. ⚠️ NO TOCAR el doble montaje de `apiRouter` en `/` y `/api` (`index.js:293-294`): es LOAD-BEARING (Traefik quita el prefijo /api; quitar el mount `/` tira producción). Accionable ya.
- [x] **`social_feed.js:318`** (PR #319 mergeada) — `page` no numérico → 500 en vez de 400.
- [x] (dark ✅ PR #332) **Sendas de campaña: falta la bendición (light)** ✅DECIDIDO+NÚMERO (2026-07-26, Claude) — la parte `dark` (maldición −25%) ya está mergeada (#332). Falta implementar la **bendición de la Senda de Luz**: al elegir senda luz, aplicar **`blessing_damage_mult = 1.20` (+20% daño de campaña) durante 24h** (usar el mecanismo existente `users.damage_multiplier`/`damage_multiplier_expiry`; añadir `light.blessing_damage_mult: 1.20` al `config.path.light` del `main-campaign.json`). Espejo temporal de la maldición dark. Toca `campaignEngine.js`. PR revisable.
  - **Parte `dark` → PR #332**: `applyCampaignDamage` ya lee `config.path.dark.curse_damage_multiplier` (0.75, del JSON, no hardcodeado) y aplica −25% al daño de campaña en la senda oscura.
  - *(Resuelto 2026-07-26: bendición = +20% daño campaña 24h vía `light.blessing_damage_mult: 1.20`; ver task de arriba.)*

---

## Secuencia sugerida

| Cuándo | Qué |
|---|---|
| **Semana 1** | Todo P0 (seguridad + promesas rotas) |
| **Semanas 2-4** | Rebalance económico + winback + día de descanso + GA4 + renombrar bosses |
| **Mes 2** | Dominio + TWA Play Store + primeros TikToks |
| **Mes 2-6** | Cadencia de contenido + Product Hunt/HN + outreach + combate con decisiones |
| **Mes 6** | Revisar kill criterion con los datos del funnel |

---

## 🗄️ P1 — Migrar la BD de Supabase a self-hosted (decisión de Roman, 2026-07-26)

> **Objetivo**: dejar de depender de Supabase. La BD pasa a estar autoalojada (misma máquina/red que Coolify, `192.168.18.175`). Roman lo quiere como plan a futuro, con discovery previo — NO empezar a migrar sin completar la fase 1 y que él apruebe el plan.

- [x] **Fase 1 — DISCOVERY (hacer primero, es solo lectura + documento)** — **PR #339 crea `docs/db-migration-discovery.md` con veredicto BAJA** (Supabase = solo Postgres vía `pg`+`DATABASE_URL`; sin Storage/Auth/RLS/realtime/extensiones). Solo faltan las **métricas de prod (a)/(c)** que requieren que Roman ejecute unas queries (van en el doc) → con eso la Fase 1 queda cerrada. Inventario original: inventariar antes de tocar nada, y volcarlo en `docs/db-migration-discovery.md`: (a) tamaño real de la BD por tabla + nº de filas + crecimiento mensual; (b) TODO lo que hoy se usa de Supabase más allá de Postgres puro (¿Storage para avatares?, ¿Auth?, ¿RLS?, ¿realtime?, ¿extensiones como `pgcrypto`/`uuid-ossp`?) — esto es lo que decide si la migración es trivial o no; (c) versión exacta de Postgres en Supabase; (d) dónde se referencia Supabase en el código (grep `supabase`) y en env vars; (e) qué se rompe si desaparece (avatares subidos, `avatar_url` apuntando a Supabase Storage…). **Entregable: el documento + un veredicto de complejidad (baja/media/alta).**
- [ ] **Fase 2 — Plan de migración (documento, tras aprobar fase 1)** — elegir topología (Postgres en contenedor de Coolify vs servicio aparte en la misma LAN), definir: backups automáticos (pg_dump programado + retención + prueba de restore, ESTO ES OBLIGATORIO antes de migrar), estrategia de secretos, y el plan de cutover con ventana y rollback. Incluir cómo se sustituye Supabase Storage si se usa (p. ej. MinIO o disco local servido por el backend).
- [ ] **Fase 3 — Postgres local en paralelo (sin cortar nada)** — levantar el Postgres self-hosted, cargar `schema.sql`, restaurar un dump reciente, y validar contra él: arrancar el backend apuntando a la BD local en un entorno de staging + pasar la suite de tests de economía (`npm test` con `DATABASE_URL` local). Producción sigue en Supabase.
- [ ] **Fase 4 — Cutover** 👤 (requiere Roman: ventana de mantenimiento + cambiar env vars en Coolify) — dump final, restore, cambiar `DATABASE_URL`, verificar, y mantener Supabase intacto ~2 semanas como rollback antes de dar de baja.
- [ ] **Al migrar, revisar el SSL de `db.js`** — hoy `rejectUnauthorized:false` está justificado por ser Supabase externo (decisión B ya documentada). Con Postgres en la red interna, reevaluar: o red privada sin TLS, o TLS con CA propio y `rejectUnauthorized:true`. Enlaza con la task de SSL ya cerrada.

## 🩸 P1 — Curva de daño: progresión exponencial + ruido por boss (investigado 2026-07-26)

> **Investigación hecha con datos reales de producción (solo-lectura)** sobre `LilAlexandru` (glvl 12, 7.332 reps), aislando el MISMO ejercicio (dips, ~45 reps por sesión) a lo largo de 3 meses. Datos de `reps.boss_damage_dealt` / `base_damage` / `gear_bonus` / `is_crit` / `boss_fight_id`.
>
> **Hallazgo 1 — la progresión NO es lineal, es explosiva**: `base_damage` por sesión de dips pasó de **3.122 (29-abr) → 13.299 (19-jun) → 101.207 (24-jun) → 223.720 (17-jul)**: ×72 en 3 meses, con un salto ×7,6 en 5 días entre el 19 y el 24 de junio. Compatible con el bucle de feedback ya identificado: la XP de FTH viene del daño infligido → más FTH → más daño → más FTH. El nerf del bono plano (#326, ×25→×5) ataca solo una parte; **el bucle (`stats.js:193`, "Parte C") sigue vivo** y es la causa de la explosión.
>
> **Hallazgo 2 — ruido de ±4× por la lotería de `weakness_stat` del boss**: el bono de debilidad compara contra el NIVEL de stat del usuario, y sus stats están muy desiguales (`str_xp` 27.495 · `end_xp` 36.660 · pero `vig_xp` 450 · `int_xp` 0 · `fth_xp` 2.287). Resultado: pelear contra Baldur (weakness `int`, stat 0) o Arthas (weakness `vig`, stat ~0) da un daño mucho menor que contra Malenia/Ornstein (weakness `str`, su stat fuerte). Sesiones consecutivas del mismo ejercicio dan 1.451 → 3.232 → 6.094 → 3.915 dmg/rep según qué boss toque. **Esto es lo que hace que "parezca que no hay progresión" y que a veces "el daño parezca bajo".**
>
> **Hallazgo 3 — `is_crit` NO explica el swing** (15-jun con crit dio 173/rep y 10-jun sin crit dio 208/rep), así que el crítico es un factor menor frente a los dos de arriba. Descartado también que sea el nerf de FE de #326: el último dato es del 25-jul y #326 se mergeó el 26-jul.

- [ ] **Romper el bucle daño→FTH→daño (Parte C del nerf de FE)** — es la causa raíz de la progresión explosiva. Que la XP de FTH NO venga del daño infligido (`backend/utils/stats.js:193`) sino de otra fuente (participación en raids/quests, como ya se decidió). ⚠️ Ojo: desnivela a todos los usuarios existentes → pensar si hace falta recalcular/normalizar stats ya acumulados. PR revisable.
- [ ] **Suavizar la lotería de weakness del boss** — que el daño no varíe ×4 según qué boss esté activo. Opciones a evaluar al implementar: (a) que el bono de debilidad tenga un suelo (nunca reducir por debajo de X% del daño base), (b) que la debilidad mire el ejercicio logueado y no solo el nivel de stat (enlaza con la task de combate ejercicio→daño ya decidida, que precisamente arregla esto), (c) rotar bosses cuyas debilidades cubran stats que los jugadores realmente entrenan (hoy hay bosses con weakness `int`, stat que nadie sube). **Recomendado: (b) + (a)**, porque (b) ya está aprobado. **NOTA (agente 2026-07-26): la opción (b) está IMPLEMENTADA en (PR #336 mergeada)** (combate ejercicio→daño): el ejercicio logueado mapea a un stat de combate y da ×1.4 vs weakness / ×0.7 vs resist en la campaña. Falta solo (a) (suelo al bono de debilidad, número por decidir) y/o (c) (rotación de bosses) si Roman los quiere además.
- [ ] **Exponer al jugador de qué depende su daño** — parte de la percepción de "daño bajo" es falta de feedback: no se ve por qué un día pegas 6.094/rep y otro 1.451. Mostrar en la UI de batalla el desglose (base · gear · debilidad del boss · crítico) para que la progresión se sienta.

## 🔍 Backlog propuesto (auditoría ingeniería/seguridad 2026-07-26)

> Re-auditoría del eje ingeniería/seguridad por la routine nocturna (todas las tasks accionables estaban ya en 🔨/⏸️/👤). Hallazgos con evidencia real; **NO implementar hasta que Roman los apruebe** (quite el ⏸️ y los mueva a su sección de prioridad). Nota positiva sin peloteo: las queries usan parámetros `$n`; las interpolaciones `${col}`/`${column}` (`roulette.js:78`, `shop.js:605/649`) vienen de allowlists server-side → **no se detectó superficie de inyección SQL**.

- [ ] (PR #333 mergeada) ✅APROBADO(2026-07-26) — **[ALTA] Deadlock latente en `getStreakStatus`/`freezeStreakForToday`**. `getStreakStatus` (`backend/utils/streak.js:64`) hace `ALTER TABLE users ADD COLUMN IF NOT EXISTS last_jackpot_week` en una conexión aparte del pool, y `freezeStreakForToday` (`backend/utils/streak.js:131`) lo llama DENTRO de una txn con `SELECT … FOR UPDATE users`. En PG16 el `ADD COLUMN IF NOT EXISTS` toma `ACCESS EXCLUSIVE` aunque sea no-op → choca con el `FOR UPDATE`; como la conexión de la txn queda `idle in transaction`, el detector de deadlock de PG no lo ve y **cuelga indefinidamente**. Verificado este run con Postgres efímero + `pg_stat_activity`. *Fix: sacar los `ALTER` de `getStreakStatus` (migración única, no por request) o no llamar a `getStreakStatus` dentro de una txn con lock. (PR #327 ya evita el patrón en el día de descanso, pero el freeze de pago sigue afectado.)*
- [ ] (PR #333 mergeada) ✅APROBADO(2026-07-26) — **[MEDIA] Migración de esquema en el hot path**. `getStreakStatus` ejecuta 2 sentencias DDL (`ensureStreakFreezeTable` + `ALTER users`) en CADA `/streak/status` y en cada recalc de stats. El DDL toma locks de tabla, es innecesario en runtime y es la causa raíz del deadlock de arriba. *Fix: mover estas migraciones a `schema.sql`/arranque y quitarlas del path de request.*
- [ ] (PR #334 mergeada) ✅APROBADO(2026-07-26) — **[MEDIA] `/auth/signup` y `/auth/google` sin rate limiting**. Solo `/auth/login` tiene limiter (`backend/auth.js:154`). `/signup` (`auth.js:105`) es no-autenticado y hace `bcrypt.hash(password, 10)` por request → vector de **DoS por CPU** (spam de altas) + creación masiva de cuentas + enumeración de emails (respuestas distintas `ERR_USER_EXISTS`/`ERR_GOOGLE_ONLY`). *Fix: limiter por IP en `/signup` y `/google` (reutilizar `utils/rateLimiters.js`).*
- [ ] (PR #335 mergeada) ✅APROBADO(2026-07-26) — **[BAJA-MEDIA] Endpoints económicos mutantes sin rate limit**. `roulette` `/spin`,`/buy-and-spin`,`/daily-spin`; `shop` `/buy/:id`,`/buy-chest/:type`,`/daily/refresh`. Tienen guardas económicas (cooldowns, checks de saldo con `FOR UPDATE`), pero sin limiter un cliente puede martillearlos (p. ej. `/daily/refresh` en bucle) y cargar la BD. *Fix: limiter suave por usuario en los mutantes de economía.*


Añadir iconos mas representativos con respecto a los enemigos de la campaña

## 🔍 Backlog propuesto (auditoría game design/economía 2026-07-26)

> Re-auditoría del eje **game design / economía** por la routine nocturna (no quedaban tasks accionables; todas en `[x]`/`🔨`/`⏸️`/`👤`). Hallazgos NUEVOS (no duplican los ya trackeados) con evidencia real y **verificados a mano** contra el código; **NO implementar hasta que Roman los apruebe** (quite el ⏸️ y los mueva a su sección de prioridad). Nota positiva sin peloteo: ruleta, apertura de cofres, shop, misiones, campaña, reto semanal y PvP tienen guardas de concurrencia correctas (`FOR UPDATE` + decremento/claim guardado) — la economía core está sólida; los leaks están en los bordes sociales/retos.

- [x] ✅ IMPLEMENTADA (2026-07-27) — **[ALTA] Auto-reto = grifo de gemas gratis** (`backend/async_challenges.js:56`). El guard anti-auto-reto es `challengerId === Number(challengedId)`, pero los IDs de usuario son `VARCHAR` (string): un `string === number` es **SIEMPRE `false`** (verificado), así que **cualquiera puede retarse a sí mismo**, registrar reps auto-reportados, ganarse el reto y cobrar hasta 2000 monedas + **10 gemas** por ciclo, repetible. Grifo de moneda premium. *Fix: `if (!challengedId || String(challengerId) === String(challengedId)) return 400;`*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] Bonus "Golpe de Gracia" pagable N veces en kill concurrente** (`backend/utils/combat.js:34` + `backend/utils/bossRewards.js:149`). El `SELECT` del boss no usa `FOR UPDATE` y el `UPDATE` de HP no filtra por estado; dos sets concurrentes pueden leer `current_hp==0` ambos → `killed=true` para los dos, y `grantLastHitBonus` fija `last_hit_user_id` **sin guard `IS NULL`**, pagando 1000-2500 monedas/ítem raro por cada llamada (+ eventos duplicados). *Fix: `AND status != 'defeated'` en el `UPDATE` de HP y derivar `killed` de la transición real (solo quien la hace cobra).*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] `POST /social/like` = grifo de monedas/CHA-XP con auto-like** (`backend/social_feed.js:724`). Toma `userId` (destino) y `date` del body sin validar destino≠emisor ni que el post exista: **crea el `daily_summary` bajo demanda** para cualquier `(TargetUserId, date)`. Con `TargetUserId = self` te auto-acuñas ~200 monedas + 200 cha_xp/día (el cha_xp infla total_xp/nivel) sin entrenar. Contradice el rediseño que redujo el ingreso no-proveniente-de-entrenar. *Fix: rechazar `TargetUserId === myUserId` y exigir que el summary ya exista (no crearlo en el like).*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] Doble pago de recompensa de racha por read-then-write no atómico** (`backend/utils/stats.js:297`). `recalculateUserStats` paga `additionalCoins` de racha con guard `lastRewardDate !== hoy` y luego escribe `last_streak_reward_date` en un `UPDATE` aparte, **fuera de transacción/lock** (solo un throttle en memoria de 10s lo frena). Dos peticiones casi simultáneas evaden el cache y pagan ambas. Valor bajo (55-200 monedas) pero es un leak real. *Fix: pago condicional dentro de `UPDATE ... WHERE last_streak_reward_date IS DISTINCT FROM $hoy RETURNING` (patrón del jackpot en `reps.js:290`).*
- *(Nota, no es hallazgo: el logueo manual aplica perks de monedas/gemas que las rutas guiada (`training.js`) y Hevy (`hevyIngest.js`) NO aplican para el mismo trabajo — inconsistencia "manual paga más", no un leak. Por si se quiere unificar algún día.)*

## 🔍 Backlog propuesto (auditoría engagement/retención 2026-07-26)

> Re-auditoría del eje **engagement / retención** por la routine nocturna. Hallazgos NUEVOS (no duplican lo ya trackeado ni los de la auditoría de economía) con evidencia real y **verificados a mano** (incl. reproducción del bug del jackpot con fechas reales de 2026); **NO implementar hasta que Roman los apruebe**. Nota positiva sin peloteo: el freeze de racha, el reto semanal, los recordatorios de referral y la limpieza de suscripciones push (410/404 + try/catch por sub) están BIEN hechos.

- [x] ✅ IMPLEMENTADA (2026-07-27; helper `getJackpotWeekKey()` en `utils/streak.js`, usada también en `reps.js`) — **[ALTA] El jackpot 5/7 se paga DOS veces (fin de semana) y se bloquea injustamente al inicio de la semana siguiente** (`backend/utils/streak.js:123-129` + `backend/reps.js:283-297`). El progreso semanal que dispara el jackpot se cuenta desde `getStartOfWeek()` (**lunes**), pero la clave anti-duplicado `last_jackpot_week` usa una fórmula casera de nº de semana (`Math.ceil((días+getDay+1)/7)`) cuyo límite cae en **sábado**, no lunes (verificado: 12-jun-2026 `W24` → 13-jun `W25`, mismo lunes-semana). Consecuencia: (1) un usuario con ≥5 días entrenados cobra el jackpot el viernes (key `W24`) y **vuelve a cobrarlo sáb/dom** de la misma semana-lunes (key ya `W25`); (2) el lunes siguiente el progreso se reinicia pero la key sigue `W25` hasta el sábado → el jackpot legítimo queda **bloqueado lun-vie**. *Fix: usar como clave el lunes de la semana (`getLocalDateString(getStartOfWeek())`, que ya se computa como `weekStart`) en ambos sitios, no la pseudo-ISO-week.*
  - ⚠️ **Efecto de una sola vez al desplegar (decisión de Roman pendiente)**: `users.last_jackpot_week` guarda valores en el formato viejo (`2026-W30`) y la clave nueva es una fecha (`2026-07-27`). Ningún valor viejo puede igualar a una clave nueva, así que **los usuarios que ya hayan cobrado el jackpot esta semana pueden cobrarlo una segunda vez** (75 monedas, una única vez). La alternativa —un `UPDATE` que fije la clave nueva a quien tenga formato viejo— evita ese sobrepago pero **bloquea el jackpot legítimo de esta semana** a quien no lo hubiera cobrado aún. Recomendación: asumir el sobrepago de 75 monedas; infrapagar una recompensa ganada es peor para retención. Si prefieres lo contrario, el `UPDATE` es de una línea.
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] Los push transaccionales ignoran el opt-out `push_disabled`** (`backend/utils/pushNotifications.js:27` + `notifications.js:65`). `sendPushNotification` solo consulta `push_subscriptions`, sin mirar `push_disabled`; los crons SÍ filtran, pero los push de alta frecuencia vía `createNotification` (LIKE, COMMENT, LEVEL_UP, NEW_CHEST, BOSS_DEFEATED, PVP) se envían **siempre**, aunque el usuario haya desactivado notificaciones → spam que empuja a desinstalar. *Fix: en `sendPushNotification`, filtrar por `push_disabled=false` antes de enviar.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] "Te han adelantado" y la tarjeta FOMO del feed usan una semana domingo-based que no cuadra con el leaderboard (lunes)** (`backend/utils/rivalReminders.js:20` + `backend/social_feed.js:68`). Ambos hacen `weekStart.setDate(getDate()-getDay())` → **domingo**, mientras el leaderboard usa `date_trunc('week')` → **lunes** (`leaderboard.js:15`). El push "X te saca N reps esta semana" se calcula sobre una ventana distinta a la clasificación que el usuario abre; los domingos sólo cuenta los reps del propio domingo → afirma adelantamientos que la clasificación contradice. *Fix: anclar la ventana del rival al lunes igual que el leaderboard.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] Winback D3/D7/D14 con igualdad exacta y sin registro de envío** (`backend/utils/winbackReminders.js:63`). `HAVING (CURRENT_DATE - MAX(r.date)) = ANY([3,7,14])` sin ledger de "enviado": si el cron falla un día (deploy/reinicio), quien estaba exactamente en día 3/7/14 pasa a 4/8/15 y **nunca recibe ese hito** (sin catch-up por rangos); y sin guardia de idempotencia, una re-ejecución reenvía todo. *Fix: rangos con ledger — `days_inactive >= stage AND NOT EXISTS(winback_sent WHERE stage=...)` + insertar el hito.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] Crons de retención N+1 secuenciales sin cota** (`backend/utils/streakReminders.js:10` + `winbackReminders.js:71`). `runStreakReminders` llama `getStreakStatus` (6 queries/usuario) en un `for … await` serial sobre todos los usuarios con push; `runWinback` hace `streakBeforeBreak` (window function sobre todo el historial) por fila, también en serie. A escala martillea la BD y retrasa los envíos. *Fix: estado de racha set-based en una query (como ya hace `rivalReminders`) + envío con concurrencia acotada.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] Toda la lógica de racha/fechas es hora-del-servidor, no por-usuario** (`backend/utils/streak.js:54-66`). Límites de día y `hoursLeftToday` se calculan en la TZ del proceso Node; para un usuario lejano la racha se rompe/persiste un día antes/después de su medianoche real y el "te quedan N h" es incorrecto. Patrón consistente en toda la app (por eso BAJA), pero daña la percepción de justicia de la racha en una app internacional. *Fix: persistir la TZ del usuario y calcular contra ella.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] La tabla `notifications` crece sin cota** (`backend/utils/notifications.js:33`). Cada like/comentario/level-up/misión/boss inserta una fila permanente; no hay poda (`DELETE FROM notifications` no existe en runtime) → la tabla crece indefinidamente y degrada el centro de notificaciones. *Fix: cron de poda (leídas > 90 días) o cap por usuario.* 
## 🔍 Backlog propuesto (auditoría SEO / rendimiento web 2026-07-27)

> Re-auditoría del eje **SEO / rendimiento web** por la routine nocturna (era el eje menos recientemente auditado junto con deuda técnica; no quedaban tasks accionables sin decisión). Foco en **rendimiento web medible** (peso de bundle/imágenes, grafo de carga real del build) porque es lo que se puede citar con evidencia y arreglar por código; el SEO on-page (canonical/hreflang/títulos por página) se **verificó y está BIEN** (ver nota positiva). Hallazgos medidos sobre un build real (`npm run build` completo, incl. `fix-ssg-seo.js`) y verificados en el grafo de `import` del bundle emitido. **NO implementar hasta que Roman los apruebe** (quite el ⏸️ y los mueva a su sección de prioridad).
>
> **Nota positiva sin peloteo**: (1) el code-splitting por ruta es correcto — las 28 vistas usan `() => import()` dinámico (`frontend/src/router.js:6-28`), 0 imports estáticos de vistas. (2) El pipeline SSG de SEO on-page funciona: tras `fix-ssg-seo.js` cada página prerenderizada lleva su **canonical, `<title>` y hreflang es/en/x-default propios** (verificado en `/es`, `/es/blog`, un post de blog y un landing). (3) Las fuentes usan el patrón no-bloqueante `media="print" onload="this.media='all'"` (`index.html:164`). El problema NO es el SEO on-page, es el **peso de la primera carga** de las páginas públicas (landing/blog), que es justo donde entra Google y el usuario nuevo.

- [x] ✅ IMPLEMENTADA (2026-07-27; `src/blogIndex.json` 43 KB vía `scripts/generate-blog-index.js`. OJO: `blogSlugs.js` importaba el corpus y lo consumían las 3 vistas, así que se dejó sin datos y `findPostBySlug` se mudó a `blogPosts.js` — sin eso el split no servía) — **[ALTA] La landing pública (`/es`, entrada nº1 de SEO) descarga TODO el corpus del blog (~261 KB JSON → chunk `blogSlugs` de 251 KB) para pintar 3 tarjetas**. `frontend/src/components/pages/Landing.vue:690` hace `import { blogPosts } from '@/blogPosts'` (import estático → el chunk `Landing-*.js` arrastra `blogSlugs-*.js`, verificado en el grafo del build) pese a que solo usa `blogPosts.slice(0, 3)` (`Landing.vue:707-714`) para 3 previews. `blogPosts.json` contiene el **cuerpo markdown completo** de los 44 posts en `locales.*.content`. Mismo patrón en `Codex.vue:342` y `BlogList.vue:173`. Penaliza LCP/TBT en la página que más importa para captar. *Fix: generar un índice ligero (slug/título/fecha/imagen/excerpt) para previews y cargar el cuerpo completo solo en `BlogView` (donde se lee el post).*
- [x] ✅ IMPLEMENTADA (2026-07-27; `defineAsyncComponent` + `v-if="isAuthenticated"`, `pusher-js` en diferido dentro de `socket.init()` y chunk propio `vendor-pusher` en `vite.config.js` — dentro de `vendor` el import dinámico no separaba nada. Landing + deps: 681 KB → 335 KB) — **[ALTA] `App.vue` importa estáticamente, NO layout público separado** — envolver los componentes/stores solo-app en `defineAsyncComponent`/import dinámico bajo `v-if="isAuthenticated"`, sin reestructurar `App.vue`; se asume que parte del vendor puede seguir colándose) — **[ALTA] `App.vue` importa estáticamente todo el runtime de la app autenticada en TODAS las rutas, incluidas landing y blog públicos**. `frontend/src/App.vue:515-536` hace import estático de `useSocketStore` (pusher-js), `useRouletteStore`, `useShopStore`, `useAudio`, `LuckyWheel.vue`, `DamageNumbers.vue`, `QuickLogSheet.vue`, `NotificationsDropdown`, `PushPrompt`, axios, etc. Confirmado en el build: `/es` (es.html) precarga `vendor` (272 KB, incl. pusher-js), `socket`, `roulette`, `shop`, `boss`, `damage`, `audio`, `notifications`. Un visitante deslogueado que llega por SEO se descarga el websocket realtime + audio + tienda + ruleta que no puede usar. *Fix: aislar los componentes/stores solo-app tras `authStore.isAuthenticated` con import dinámico / `<component :is>` o un layout público separado, para que las páginas públicas envíen un bundle mínimo.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] Se sirven imágenes de varios MB sin optimizar, y una card usa el GIF pesado teniendo el webp al lado**. `frontend/src/components/social/ChallengeActivityCard.vue:175` referencia `/images/pvp/dark_arena.gif` (**3,5 MB**) —y otros GIFs del array— cuando existe `dark_arena.webp` (**103 KB**, ×34 más ligero) que ya usan `PvpBattleView.vue`/`PvpConfigModal.vue`. Además `dist/images/bosses/coloso_volcanico.png` = **2,9 MB** y `dist/images/blog/dashboard_mockup.png` = **1,0 MB** sin versión webp. Total imágenes en `dist`: **13,7 MB / 175 ficheros**. *Fix: usar el webp existente en la challenge-card (o webp animado), y convertir `coloso_volcanico.png`/`dashboard_mockup.png` a webp.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] Las imágenes hero del blog se hotlinkean desde `images.unsplash.com` — el LCP de cada post vive en un tercero**. Los campos `image` de `blogPosts.json` apuntan a `images.unsplash.com/...?w=1200&q=80` y la página de post prerenderizada las carga (verificado). El elemento LCP del blog depende de un CDN externo (DNS/TLS/conexión extra pese al preconnect, sin control de caché/formato, fuga de referrer). También se usa Unsplash como fallback en `Landing.vue:474`, `Codex.vue:265,464`, `BlogView.vue:462`, `BlogList.vue:194`. *Fix: descargar y auto-alojar como webp local con dimensiones explícitas.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA-MEDIA] Falta lazy-load y dimensiones intrínsecas en la mayoría de imágenes (CLS + ancho de banda)**. Solo **11 de 46** `<img>` del frontend usan `loading="lazy"` y solo **4 de 46** declaran `width/height` (o aspect-ratio). Las imágenes below-the-fold (thumbnails de blog, arte de bosses, listas de avatares) se cargan de forma eager y no reservan espacio → salto de layout (CLS penaliza Core Web Vitals → ranking). *Fix: `loading="lazy"` + dimensiones intrínsecas en imágenes que no sean el hero.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] Cinco familias de Google Fonts con muchos pesos desde el CDN de Google**. `index.html:163-166` carga Inter (6 pesos), Inter Tight (variable itálica), JetBrains Mono (3), Barlow Condensed (4) y DM Mono (2) desde `fonts.gstatic.com` (tercero en la ruta de render, mitigado por el swap `print/onload`). *Fix: subconjuntar + auto-alojar (o eliminar familias/pesos no usados). BAJA porque el swap ya evita el bloqueo de render.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] Dos propiedades GA4 duplican cada evento**. `index.html:149-150` hace `gtag('config', 'G-TJ4SF4KS8R')` y `gtag('config', 'G-3SL67W0LS6')`. Si una es legacy, sobra; si no, cada pageview/evento se envía dos veces (doble red + datos sesgados). *Fix: confirmar cuál es la propiedad viva y quitar la otra.*

## 🔍 Backlog propuesto (auditoría deuda técnica 2026-07-27)

> Re-auditoría del eje **deuda técnica** por la routine nocturna (era el eje menos recientemente auditado: 2026-07-23 vs. 07-26/07-27 de los demás). Todas las tasks accionables estaban ya en `[x]`/`🔨`/`⏸️`/`👤`, así que el run se convirtió en auditor. Hallazgos NUEVOS (no duplican lo ya trackeado: ni los archivos-monstruo, ni las 3 fuentes de esquema, ni JWT, ni el rate-limit, ni los N+1 de crons ya listados) con **evidencia real verificada a mano** (código leído, `archivo:línea`, contrastado contra los `CREATE INDEX` reales de `schema.sql` **y** los de arranque/`/db/init` para no dar falsos positivos). **NO implementar hasta que Roman los apruebe** (quite el ⏸️ y los mueva a su sección de prioridad).
>
> **Nota positiva sin peloteo**: (1) los 6 crons (`index.js:67-105`) encadenan `.catch()` y no tumban el proceso; existe un error-middleware global (`index.js:779`) que recoge las rutas async sin try/catch de Express 5. (2) Pusher (`socketManager.js`), web-push y el cliente Hevy (`hevy.js`) van todos envueltos en try/catch; las notificaciones fire-and-forget capturan su propio error y nunca rechazan. (3) La gestión de listeners/timers del frontend es en general disciplinada (limpieza en `onUnmounted` en Dashboard/Inventory/Shop/Missions/PvpBattleView…); los stores comparten un buen patrón `fetchPromise`+`try/catch/finally`. (4) El feed usa `MATERIALIZED CTE` para paginar antes de calcular stats ricos — buen patrón. (5) `"test"` ya es `node --test` real y `/db/init` gatea bien por `DB_INIT_SECRET`. Los problemas son de **índices ausentes**, **duplicación** y **higiene de arranque/config**, no de arquitectura.

### Rendimiento de BD — índices ausentes (lo de mayor impacto)

- [x] ✅ IMPLEMENTADA (2026-07-27; en `ensureSchemaMigrations()` y en `/db/init`, aplicados uno a uno con try/catch porque `notifications` no existe en `schema.sql`) — **[ALTA] Índice ausente en `notifications(user_id, created_at)`**. La tabla `notifications` se crea en `backend/index.js:630-640` **sin ningún índice** (el `idx_notifications_user_id_created` solo vive en `backend/archive/apply_perf_indices.js`, que NO corre en runtime). Cada carga/poll del centro de notificaciones ejecuta `WHERE n.user_id = $1 ORDER BY n.created_at DESC LIMIT 30` (`backend/notifications.js:13-19`) → **seq scan + sort de toda la tabla** en una ruta muy frecuente, sobre una tabla que además crece sin cota (ya trackeado en engagement). Es la deuda de índice más clara. *Fix: `CREATE INDEX idx_notifications_user_created ON notifications(user_id, created_at DESC)` en `schema.sql` + arranque.*
- [x] ✅ IMPLEMENTADA (2026-07-27; en `schema.sql`, `ensureSchemaMigrations()` y `/db/init`. El de `friendships` NO entra, sigue pendiente abajo) — **[ALTA] Índice ausente en `users(total_reps)`** (columna denormalizada, `schema.sql:10`; sin índice en ninguna de las 3 fuentes). Se usa para ranking global en subqueries **correlacionadas por cada fila del feed** (`backend/social_feed.js:672` `COUNT(*) … WHERE u2.total_reps > f.total_reps` y `:674-678` `next_rank_rival ORDER BY total_reps`), y también en `backend/reps.js:534` (weekly-summary) y `backend/profile.js:107`. → seq scan de `users` repetido hasta 10× por render del feed. *Fix: índice parcial `CREATE INDEX idx_users_total_reps ON users(total_reps DESC) WHERE is_private = false`.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] `friendships` sin índice utilizable por `user_id_2`**. El único índice (`schema.sql:253`, `index.js:628`) es `(user_id_1, user_id_2)`; las queries de amigos hacen `SELECT user_id_1 FROM friendships WHERE user_id_2 = $1` (`backend/leaderboard.js:104`, `backend/social_feed.js:342-343`) → un compuesto liderado por `user_id_1` **no sirve** para filtrar por `user_id_2`, seq scan en la mitad UNION. *(Nota menor del mismo tipo: `user_missions(user_id)` tampoco tiene índice canónico y se filtra en `utils/missions.js:15-20`.)* *Fix: `CREATE INDEX idx_friendships_user2 ON friendships(user_id_2)`.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] `GET /reps` sin `LIMIT`** (`backend/reps.js:30-40`): `SELECT ... FROM reps WHERE user_id=$1 ORDER BY date DESC` carga y serializa el **historial completo** de reps del usuario en cada llamada; crece sin cota con la antigüedad de la cuenta. (`GET /reps/stats` `:416-419` hace lo mismo con `SELECT DISTINCT date` solo para calcular la racha en JS.) *Fix: paginar/limitar, o calcular la racha con una query acotada.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] Índice `idx_reps_user_date` redundante** (`schema.sql:252`): `reps` ya tiene `UNIQUE(user_id, date, exercise_type)` (`schema.sql:50`), cuyo btree con prefijo `(user_id, date)` sirve a las mismas lecturas. El índice extra solo añade coste de escritura en la tabla más caliente sin beneficio. *Fix: `DROP INDEX idx_reps_user_date`.*

### Robustez de arranque y proceso

- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] Sin handlers de proceso `unhandledRejection`/`uncaughtException` + bootstrap sin `.catch()`**. No hay ningún `process.on('unhandledRejection'|'uncaughtException')` en todo `backend/` → en Node moderno una promesa rechazada no capturada tumba el proceso sin log limpio. Y el arranque `Promise.all([ensureSchemaMigrations(), ensureAllTrainingExercisesExist()]).then(() => app.listen(...))` (`backend/index.js:771`) **no tiene `.catch()`**: si `ensureAllTrainingExercisesExist()` rechaza (blip de Postgres al bootear), `app.listen` nunca corre y el fallo sale opaco. *Fix: handler de proceso que loguee y `process.exit(1)` + `.catch()` en el bootstrap.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] Sin validación fail-fast de env al arranque; `JWT_SECRET` revienta en la 1ª petición, no al boot**. `backend/auth.js:14` y `backend/middleware.js:13,38` usan `process.env.JWT_SECRET` sin default ni chequeo; si falta, el proceso arranca "sano" y falla en la primera request autenticada. Solo Pusher valida (`index.js:234`). *Fix: módulo que verifique `JWT_SECRET`/`DATABASE_URL`/`GOOGLE_CLIENT_ID` al inicio y `process.exit(1)` si faltan.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] `JSON.parse(fight.allowed_exercises)` sin try/catch local** (`backend/utils/pvp_logic.js:30`). Está **contenido** por el try/catch de la ruta (`pvp.js:141-226` → ROLLBACK + 400), no crashea el proceso, pero es **inconsistente**: el resto de `JSON.parse` sobre datos de BD sí van protegidos (`stats.js:81`, `users.js:208`, `shop.js:96,217`, `pushNotifications.js:44`). *Fix: guard local que devuelva error de validación en vez de lanzar.*

### Mantenibilidad del frontend

- [ ] ✅ APROBADA (2026-07-27, Roman: **incluye pasar los labels a i18n es/en**, absorbiendo el hallazgo MEDIA de abajo) — **[ALTA] Lógica de rareza (label/color/clase CSS) duplicada y divergente en 10+ componentes** sin composable compartido (no existe nada de rareza en `utils/` ni `composables/`): `Inventory.vue:1373/1380/1240/828`, `social/ActivityCard.vue:488/698`, `shop/ChestOpening.vue:366/378/390`, `pages/Shop.vue:987`, `pages/AdminPanel.vue:403`, `PublicProfile.vue:199-200`, `WeeklyShareCard.vue:264-265`, `NodeBattleView.vue:71`, `BossHealth.vue:296`, `PotionUseModal.vue:81`. Cada copia trata `epic`/`especial`/`cosmico` distinto y ya hay inconsistencias reales (ver siguiente). *Fix: un `useRarity()` (o `utils/rarity.js`) único con label i18n + clases, y consumirlo en todos.*
- [ ] ✅ APROBADA (2026-07-27, Roman: **absorbida por la task de `utils/rarity.js` de arriba** — se arregla en el mismo PR) — **[MEDIA] Strings de rareza hardcodeados en español que ignoran i18n** (viola la convención de `CLAUDE.md`: textos de UI por i18n en ambos locales). `shop/ChestOpening.vue:392-397` devuelve `'COMÚN'/'RARO'/'ESPECIAL'/'LEGENDARIO'/'CALISTÉNICO'` fijos (y `:69` `'RECURSOS'`); `Inventory.vue:1389` devuelve `'CÓSMICO'` en medio de un helper que por lo demás usa `i18n.t`. Un usuario en inglés ve español en las pantallas de recompensa (abrir cofre). *Fix: mover a claves `rarity_*` en `locales/es.js` y `en.js`.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] `stores/training.js` sin manejo de error en sus axios** (único store así; el resto usa `try/catch/finally`): `fetchPlans` `:23`, `fetchCustomPlans` `:28`, `createCustomPlan` `:39`, `fetchMine` `:57`, etc. Delega el manejo a llamadores inconsistentes: `GoalOnboardingModal.vue:168/186` y `TodayWorkout.vue:558` **no** capturan → promesa rechazada no capturada / UI en carga colgada si falla la red. *Fix: alinear con el patrón `try/catch/finally` de los demás stores.*

### Higiene de config, logs y cruft

- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] `console.log` de debug por-petición en prod (ruido + IDs de usuario en logs)**: `profile.js:137` (`[PROFILE_API] INCOMING - ID: ...` en cada visita a perfil), `:203`, `:312`; `shop.js:145` (`GET /daily for userId: ...` en cada carga de tienda), `:612`. (~161 `console.log` en 40 archivos; la mayoría son `[CRON]`/`[BOOT]` intencionales, estos no.) *Fix: quitar o gatear por `NODE_ENV`. También `index.js:159` `morgan('dev')` es incondicional en prod → usar `combined` o gatear.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] Cruft versionado + config menor** (agrupados; todos verificados con `git ls-files`): (1) archivos muertos trackeados en git: `backend/backend_debug.log`, `backend/tmp/update_prices.cjs`, `backend/tmp/update_prices_pg.js`, `frontend/build_output.txt`, `frontend/build_output_utf8.txt` (`tmp/` no está en `.gitignore`). (2) Componentes muertos (0 refs): `frontend/src/components/misc/HelloWorld.vue`, `system/EasterBackground.vue`. (3) Deps: `pusher ^5.2.0` declarada en `package.json` **raíz** (redundante y con versión dispar vs `backend/package.json ^5.3.3`), y `gifuct-js ^2.1.2` declarada y **sin usar** en `frontend/package.json:22`. (4) Sin campo `engines.node` en ningún `package.json` pese a que los Dockerfiles fijan `node:22`. (5) `.env.example` ausente pese a >12 env vars críticas usadas. *Fix: borrar los muertos + `.gitignore` de `tmp/`/`build_output*`; quitar deps sobrantes; añadir `engines` y un `.env.example`.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] Fugas latentes de listener/timer en el frontend** (impacto real bajo, App es raíz): `stores/theme.js:80` registra `matchMedia.addEventListener('change', () => …)` con arrow **anónima** que nunca se remueve (irremovible + se apila si `init()` se re-ejecuta); `App.vue:743` crea `setInterval(…30000)` de presencia que solo se limpia en su propio callback al desloguear, no en el `onUnmounted` (`:768`, que solo limpia `rouletteClock`). *Fix: guardar la referencia del handler y limpiarla; añadir `clearInterval(pingInterval)` al `onUnmounted`.*

## 🔍 Backlog propuesto (auditoría ingeniería/seguridad 2026-07-27)

> Re-auditoría del eje **ingeniería/seguridad** por la routine nocturna (no quedaban tasks accionables sin decisión: todas en `[x]`/`🔨`/`⏸️`/`👤` o congeladas por 🧊). Foco en las superficies **no cubiertas** por la auditoría de seguridad del 2026-07-26 (que cerró rate-limiting, deadlock de racha y DDL en hot-path): **control de acceso / IDOR, validación de entrada type-confusion, y manejo de secretos/cripto/datos externos**. Hallazgos NUEVOS (no duplican lo ya trackeado) con **evidencia real verificada a mano** (código leído, `archivo:línea`, exploit concreto). **NO implementar hasta que Roman los apruebe** (quite el ⏸️ y los mueva a su sección de prioridad).
>
> **Nota positiva sin peloteo** (verificado, no es padding): (1) el **núcleo económico está sólido** — shop `/buy`,`/buy-chest`,`/daily/refresh`,`/daily/claim`, roulette, boss chests, misiones y campaña usan todos `withTransaction` + `FOR UPDATE` + descuentos guardados y precios server-side; no se encontró faucet nuevo. (2) **Sin superficie de inyección SQL**: todo valor va por `$n`; los identificadores `${col}` (`shop.js:587,604,647`, `roulette.js:78`, `challenges.js:50`) vienen de allowlists server-side. (3) **Sin mass-assignment**: `PATCH /users/profile` (`users.js:64`) usa allowlist manual de campos, no hace spread de `req.body`. (4) **Admin bien gateado**: `admin.js` monta TODAS las rutas tras `authenticate, isAdmin`; `/test` solo si `testEndpointsEnabled`; `/db/init` tras `DB_INIT_SECRET`. (5) **Cripto correcta**: AES-256-GCM con IV aleatorio de 12 bytes por cifrado + auth tag verificado (`hevyCrypto.js:12-31`); avatares custom se rasterizan a WebP 128×128 vía `sharp` (`users.js:112-145`) → no hay XSS por SVG; Google OAuth valida `audience` (`auth.js:49`); el webhook de Hevy usa token UUIDv4 y sólo importa workouts reales de la cuenta de la víctima → no permite inyectar datos forjados. Los problemas están en **PvP** (que no tiene guardas de participante consistentes) y en **higiene de validación/secretos**, no en la economía.

### Control de acceso / IDOR

- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] `POST /pvp/:id/finish` no comprueba que el que llama sea participante** (`backend/pvp.js:271-292`). El handler carga la pelea y sólo valida `fight.status !== 'active'`; nunca comprueba `req.user.id === challenger_id || challenged_id` antes de resolverla (`UPDATE ... status='completed', winner_id=$1`). Los IDs de pelea son visibles en el feed. Exploit: cualquier autenticado hace `POST /api/pvp/<id-de-pelea-ajena>/finish` y **fuerza el settlement de un duelo entre otros dos jugadores** (ganador según HP/daño en ese instante), fabricando resultados win/loss que salen en el feed social. Sin payout de moneda en PvP → es integridad/griefing, no economía. `/accept` (`:85`) y `log-set` (`pvp_logic` vía `:150`) SÍ tienen el guard; `finish` se lo saltó. *Fix: `if (fight.challenger_id !== req.user.id && fight.challenged_id !== req.user.id) return res.status(403)`.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] PvP: el cliente fija `anticheat_enabled` y `max_hp` → victoria garantizada** (`backend/pvp.js:40,53` + `backend/utils/pvp_logic.js:38-48`). `POST /pvp/challenge` guarda `anticheat_enabled = (anticheatEnabled !== false)` **tal cual lo manda el retador**, y el tope de reps por set (`pullups:15`, etc.) sólo se aplica `if (fight.anticheat_enabled)`; con `anticheatEnabled:false` el único límite es `reps <= 0` (`pvp_logic.js:50`) → `reps: 999999` → `damage = 999999·18`. Sumado a `max_hp` sin validar (`maxHp || 1000`, se puede fijar `1`), el retador **one-shotea a quien acepte**. Un `maxHp` no numérico además revienta con 500 en el INSERT a columna integer. Sin economía en PvP → integridad del modo. *Fix: forzar `anticheat_enabled = true` server-side (o gatearlo), y validar `maxHp`/`timeLimit` como enteros acotados + coaccionar `reps` a entero acotado en `log-set`.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] Lecturas y confetti de PvP sin gate de participante** (`backend/pvp.js:112` `GET /:id`, `:246` `GET /:id/events`, `:230` `POST /:id/confetti`). Toman un `id` de pelea y actúan sin comprobar participante ni privacidad: cualquier autenticado lee el estado completo y el stream de sets (ejercicio/reps/daño) de duelos ajenos e inyecta eventos de confetti en cualquier pelea. Info-disclosure menor + spam de eventos. *Fix: gatear lecturas/acciones a que el llamante sea participante, o declarar los reads público-por-diseño y aplicar el guard sólo al confetti.*

### Validación de entrada / robustez

- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] Params numéricos de ruta y `goalValue` sin guarda de tipo → 500 en vez de 400** (`backend/shop.js:389,581,663,376`; `backend/boss.js:172,635`; `backend/missions.js:139`; `backend/async_challenges.js:62`). Varios handlers hacen `parseInt(req.params.id)` sin `Number.isFinite`, o comparan un valor sin castear: en `async_challenges.js:62` `goalValue < 10 || goalValue > 100000` deja pasar `"abc"` (ambas comparaciones `false`) y floats → `calcReward` da `NaN` y el INSERT a columna integer revienta con 500. Sin bypass económico (precios/ownership siguen server-side) — es DoS-lite / status incorrecto. Nota: `shop.js:84,109` ya lo hacen bien con `Number.isFinite`, así que es inconsistencia, no vacío sistémico. *Fix: `const n = Number(x); if (!Number.isInteger(n) || fuera_de_rango) return 400;` en todos.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] `PATCH /users/profile` no valida longitud/rango de `name`/`daily_goal`/`body_weight`** (`backend/users.js:76-86`). El allowlist de campos está bien (sin mass-assignment), pero `name` (`:77`) se persiste sin tope de longitud y `daily_goal`/`body_weight` (`:81,:85`) sin validación numérica ni rango. Vue escapa el HTML y no hay render server-side (sin XSS), pero permite persistir nombres gigantes / valores basura que luego se propagan a presence de Pusher, push y feed. *Fix: cap de longitud a `name` (p. ej. ≤40) y validar `daily_goal`/`body_weight` como números en rango sano.*

### Secretos / cripto / config

- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[MEDIA] La clave de cifrado de las API keys de Hevy tiene un fallback hard-coded en el CÓDIGO** (`backend/utils/hevyCrypto.js:8`). `const SECRET = process.env.HEVY_ENCRYPTION_KEY || process.env.JWT_SECRET || 'reppy-hevy-dev-secret'`. Complementa (no duplica) la nota infra ya cerrada [fijar `HEVY_ENCRYPTION_KEY` en Coolify]: aquí el hallazgo es **de código**. Si el env falta, cada `users.hevy_api_key` queda cifrada bajo una **constante pública del repo** → cualquier dump de BD se descifra offline recomputando `sha256('reppy-hevy-dev-secret')`; y la rama `|| JWT_SECRET` reutiliza el secreto de firma de auth para cifrado en reposo (viola separación de claves: una fuga de `JWT_SECRET` también rompe las keys de Hevy). *Fix: `const SECRET = process.env.HEVY_ENCRYPTION_KEY; if (!SECRET) throw new Error('HEVY_ENCRYPTION_KEY required');` — quitar ambos fallbacks (fail-fast, encaja con la task de validación de env al arranque).*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] `/db/init` acepta el secreto por query-string y lo compara sin tiempo constante** (`backend/index.js:383-384`). `const provided = req.get('x-init-secret') || req.query.secret;` → un `?secret=...` se filtra a logs de acceso, logs de proxy y `Referer`; y `provided !== expected` no es constant-time (fuga marginal por timing). *Fix: aceptar el secreto sólo por header y comparar con `crypto.timingSafeEqual`.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] CSP con `'unsafe-inline'` y `'unsafe-eval'` en `script-src`** (`backend/index.js:147`). Helmet/CSP está presente, pero `script-src` permite inline+eval → la CSP no aporta ninguna mitigación de XSS; cualquier DOM-XSS en la SPA ejecuta sin barrera (los campos de usuario sólo los protege el auto-escape de Vue). *Fix: quitar `'unsafe-inline'`/`'unsafe-eval'` y usar nonces/hashes para GA; o documentar que la CSP no es capa de defensa aquí.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] `workoutId` sin `encodeURIComponent` en el fetch a Hevy** (`backend/utils/hevyClient.js:36`). `hevyFetch(apiKey, \`/workouts/${workoutId}\`)` interpola crudo un id que viene del body del webhook; los hermanos `:51`/`:56` sí codifican. No es SSRF (host fijo `api.hevyapp.com` + requiere token de webhook válido), pero es construcción de request saliente sin sanear (inyección de path/query). *Fix: `encodeURIComponent(workoutId)`.*
- [ ] ⏸️ PENDIENTE DE APROBACIÓN — **[BAJA] Semilla de PvP con `Math.random()` truncado** (`backend/pvp.js:48`). `const seed = Math.random().toString(36).substring(7)` → ~4-5 chars de entropía, no-cripto y predecible; si la semilla dirige el RNG determinista de combate (`seededRandom` en `pvp_logic.js`), un oponente podría anticipar los rolls de crítico. El resto de tokens de la app sí usan `crypto` (`crypto.randomUUID`/`randomBytes`), éste es el único caso débil. *Fix: `crypto.randomBytes(8).toString('hex')`.*
